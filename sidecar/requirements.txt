# Runtime dependencies for the CS2 rank sidecar, pinned so the frozen build is
# reproducible. steam (ValvePython) speaks the Steam CM protocol; the sidecar
# supplies a WebSocket transport for it (websocket-client) because Steam's TCP
# CM is filtered on some connections while the WebSocket CM is not.
steam[client]==1.4.4
websocket-client==1.9.0
